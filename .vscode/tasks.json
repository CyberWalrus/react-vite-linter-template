{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Watch Typed SCSS Modules",
            "type": "shell",
            "command": "yarn ts:scss:watch",
            "isBackground": true,
            "problemMatcher": [],
            "runOptions": {
                "runOn": "folderOpen"
            },
            "options": {
                "cwd": "${workspaceFolder}/src"
            }
        }
    ]
}